describe("recommendationService", () => {
  test.todo("returns 1 transfer when it is optimal");
  test.todo("returns 2 transfers when it is still good and there are 2 available");
  test.todo("returns 2 transfers when they are more than twice as good as 1");
});
